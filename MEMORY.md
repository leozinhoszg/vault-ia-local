# MEMORY — estado do vault

- **Última atualização:** 1º de setembro de 2026.
- **Escopo:** IA local para casa e empresa; hardware, software, modelos, RAG, treinamento, operação e governança.
- **Dono editorial:** Luiz Guimarães; substituto e dono técnico ainda precisam ser designados.
- **Fonte de verdade de hardware:** `03-Hardware/` e documentação oficial do fabricante.
- **Fonte de verdade de modelos:** model card/repositório oficial; blogs editoriais são contexto, não autoridade única.
- **Fonte de verdade de governança:** NIST AI 600-1 e políticas internas aprovadas.
- **Estado de revisão:** builds e preços são planejamento e precisam de cotação brasileira na data de compra; compatibilidade AMD exige consulta à matriz ROCm vigente.
- **Pendências:** designar substituto, revisor de segurança e operador de benchmarks; preencher região/tarifa de energia, orçamento real, máquina atual e conjunto de benchmarks do usuário.

## Changelog

- **2026-09-01 (rev. 2, re-auditoria P0):** corrigido `Break_even!E2:E4` da planilha TCO (faltava multiplicar o custo blended pelo câmbio; o resultado era ~5,5× maior que o texto); `Local!C2` passou a descontar o valor residual; fator Batch e fatores de contexto longo por categoria agora entram no cálculo; aba `Sensibilidade` ampliada para câmbio, vida útil, utilização e tarifa. Ficha do Qwen3-Coder 30B passou a citar o model card oficial (`Qwen3-Coder-30B-A3B-Instruct`). Script RAG ganhou `--selftest` e `--retrieve-only`; selftest executado em Windows/Python 3.11 com chromadb 1.0.20. Lockfile com hashes adicionado. Cabeçalhos `## Referências` e datas normalizados em 21 notas; validador passou a distinguir "Markdown no pacote" de "analisados" e a aceitar justificativas. Impacto: [[00-Inicio/Auditoria-P0]] reescrita; nenhuma recomendação de hardware ou modelo mudou. Fonte: re-auditoria externa + verificação com `openpyxl` e recomputação em Python.
- **2026-09-01 (rev. 3, fechamento do gate):** RAG reproduzido em venv limpo a partir do lockfile com hashes (selftest + embedding real; Ollama não executado) — evidência em [[07-Implementacao-Casa/Evidencias/RAG-reproducao-2026-09-01]]; planilha TCO ganhou aba `Checks`, anuidade opcional, tabelas estruturadas e valores recalculados pelo Excel (`99-Templates/recalcular_tco.ps1`, conferido por `99-Templates/check_tco.py`); CI em `.github/workflows/validate.yml` (validador `--strict`, índice `--check`, sha256 do lockfile, `check_tco.py`, selftest, release por tag). Itens que exigem hardware (benchmarks, fichas medidas, BOM real, aceitação empresarial) continuam não iniciados e estão listados em [[00-Inicio/Auditoria-P0]].
- **2026-09-01 (rev. 4, enriquecimento de modelos):** novas fichas [[02-Modelos/Fichas/Laguna-XS-2.1]] (33B/3B, 262.144, OpenMDW-1.1, GGUF Q4_K_M de 20,3 GB, Ollama local, llama.cpp PR #25165 mesclado em 2026-07-22) e [[02-Modelos/Fichas/Kimi-K2.7-Code]] (1T/32B, 256K, INT4 nativo de 595 GB, Modified MIT, Ollama apenas `:cloud`; o card não publica SWE-bench). Nova nota [[02-Modelos/Local-real-vs-cloud]] com os quatro modos de execução (local real, offload CPU/RAM, serviço remoto via ferramenta local, Ollama Cloud) e `OLLAMA_NO_CLOUD=1`. Nova [[02-Modelos/Tabela-normalizada-de-benchmarks]] com variante, harness, métrica, tentativas, ferramentas, contexto e data de captura para Laguna XS/S 2.1, Kimi K2.6/K2.7 Code, Qwen3.6-27B e Qwen3-Coder-Next. Ficha padronizada e catálogo ganharam os campos "Modo de execução" e "Arquivo quantizado real". [[02-Modelos/Verificacao-PromptQuorum]] passou a rejeitar `ollama run kimi-k2.6` como prova de inferência local (única tag oficial é `:cloud`), registrou as três correções (1T ≠ 22 GB; 58,6 Pro ≠ 77,2 Verified) e a regra de data de captura por causa das datas internas conflitantes da página. Impacto: nenhuma recomendação de hardware mudou; Laguna XS 2.1 entra como candidato de GPU de 24 GB, Kimi K2.7 Code como infraestrutura/remoto. Fonte: model cards e páginas oficiais capturados em 2026-09-01; nenhum benchmark próprio executado.
- **2026-09-01 (rev. 3b, RAG ponta a ponta):** Ollama 0.33.2 instalado (winget) com `qwen3.5:4b`; pipeline completo reproduzido no venv limpo com 3/3 respostas corretas e citadas, incluindo recusa sem evidência. Defeito corrigido no script: thinking + `num_ctx` 4096 zeravam a resposta (agora `think=false`, `--num-ctx 8192`, exit 3 em resposta vazia). Evidência em [[07-Implementacao-Casa/Evidencias/RAG-reproducao-2026-09-01]].
- **2026-09-01 (rev. 6, camada para iniciantes):** seis notas didáticas novas — [[01-Fundamentos/00-Comece-aqui]] (o que acontece ao apertar Enter, passo a passo, com a peça de hardware de cada passo), [[01-Fundamentos/Glossario-essencial]] (~35 termos com analogia, "por que importa" e link para a nota profunda), [[01-Fundamentos/Parametros-e-tokens]] (200M × 24B, totais × ativos, o que parâmetro tem e não tem a ver com token), [[01-Fundamentos/Entrada-e-saida]] (prompt, contexto nativo × `num_ctx`, prefill × decode, contagem com o exemplo real de 358/48 tokens), [[01-Fundamentos/Carregar-um-peso]] (SSD → RAM → VRAM, três cenários de `ollama ps`, tabela momento × SSD/RAM/VRAM/CPU/GPU) e [[03-Hardware/Por-que-VRAM-RAM-e-CPU-importam]] (o que cada peça compra e não compra, tabela sintoma → recurso, ordem de prioridade de compra como opinião editorial). Ligadas no MAPA (passo 0), Livro, README e "ver também" de LLM-e-inferencia, GPU-vs-CPU-vs-NPU e Calculadora. Fontes novas verificadas em 2026-09-01: docs.ollama.com/faq e /gpu (os docs saíram do GitHub), GGUF spec, llama.cpp build.md, HF LLM Course. Impacto: nenhuma recomendação de hardware ou modelo mudou; números reaproveitados da Calculadora, do Sizing, do Comparativo e da evidência do RAG. Regras de bolso de tokens por palavra em pt-BR estão marcadas como estimativa editorial.
- **2026-09-02 (rev. 8b, correção do CI):** o `validate-vault` estava vermelho desde antes da rev. 7 (todas as execuções recentes em `main` falhavam) no passo de SCA: o pip-audit em modo `require-hashes` invoca o pip interno no runner Ubuntu, que aplica os markers de Linux do `torch==2.13.0` e exige hash de `cuda-toolkit==13.0.3` e `nvidia-*-cu13` — dependências que o lock, gerado no Windows, legitimamente não contém (verificado no metadata do torch 2.13.0 no PyPI em 2026-09-02; `cuda-toolkit` é metapacote oficial NVIDIA, não typosquatting). Correção em duas iterações: remover `require-hashes` não bastou (qualquer linha `--hash` no arquivo liga o modo hash do pip interno do pip-audit); o SCA passou a auditar uma cópia do lock só com `nome==versão` (`no-deps`), gerada no próprio workflow; a integridade do lock continua garantida pelo passo `sha256sum -c`. Pendência estrutural registrada em [[00-Inicio/Auditoria-P0]]: lock multi-plataforma. Nenhum conteúdo editorial mudou.
- **2026-09-02 (rev. 8, camada de controles verificáveis):** o vault ganhou a cadeia `risco → requisito → configuração → teste → evidência → operação → revisão`. Novas notas: [[10-Operacao-e-Seguranca/Threat-model-LLM-local]] (ativos, cinco fronteiras de confiança, trifecta letal, STRIDE × OWASP LLM aplicado a LM Studio/Ollama/RAG/MCP), [[99-Templates/Modelo-de-nota-de-controle]] (frontmatter `control_id` com escala honesta documented→configured→tested→verified; tested/verified exigem `evidence_type: own-test`), [[08-Implementacao-Empresa/Matriz-RBAC-e-ABAC]] (seis papéis × nove permissões, default deny, break-glass, identidade delegada), [[10-Operacao-e-Seguranca/Esquema-de-auditoria-LLM]] (17 campos, prompt/resposta fora do esquema padrão, trilha ≠ log operacional), [[10-Operacao-e-Seguranca/Supply-chain-de-modelos]] (manifesto por artefato, digest fixado, sem latest/auto-pull). Validador estendido: schema de nota de controle (status, evidence_type, datas ISO, seções obrigatórias, `CONTROL_REVIEW_OVERDUE` justificável com `revisao-vencida`), testado com nota propositalmente malformada (12 erros detectados; corrigido bug de `\s*` capturando linha seguinte em campo vazio). **Primeiras evidências de laboratório de segurança** ([[10-Operacao-e-Seguranca/Evidencias/Ollama-testes-negativos-2026-09-01]], Ollama 0.33.2 real): bind loopback PASS; header Authorization ignorado (200 com token inválido) confirmando ausência de auth nativa; `Ollama cloud disabled: false` — **divergência aberta**: esta máquina não aplica o `OLLAMA_NO_CLOUD=1` que o vault recomenda (correção é decisão do dono editorial); default real de `OLLAMA_ORIGINS` inclui `0.0.0.0`, `app://*`, `file://*` e webviews — mais amplo que a FAQ sugere. Três controles piloto em `10-Operacao-e-Seguranca/Controles/` (OLL-NET-001 tested/PASS, OLL-AUTH-002 tested, OLL-CLD-003 tested/FAIL honesto). Origem: plano de auditor externo verificado e enxugado (duplicações removidas; "verified sem teste" rejeitado). Impacto: nenhuma recomendação mudou; a novidade é que parte delas agora tem prova. Pendentes: testes de LM Studio (não instalado), gate de produção, RAG/MCP (P1), testes da matriz RBAC (exigem gateway).
- **2026-09-01 (rev. 7, runtimes de desktop em empresa):** nova nota [[08-Implementacao-Empresa/04-LM-Studio-e-Ollama-como-runtimes-internos]] — arquitetura IdP + gateway/policy engine na frente de LM Studio/Ollama, checklist de hardening. Fatos verificados em 2026-09-01 nas docs oficiais: LM Studio 0.4.0+ tem `Require Authentication` e API Tokens com permissões editáveis; settings `Serve on Local Network`, `Enable CORS`, `Allow per-request MCPs`, `Allow calling servers from mcp.json` e os três controles de JIT; `/api/v1/chat` persiste conversa por padrão (`store` default true → exigir `store:false` para dado sensível); daemon `llmster` com startup task systemctl. Ollama sem autenticação nativa na API local; env vars `OLLAMA_HOST` (default `127.0.0.1:11434`), `OLLAMA_MODELS`, `OLLAMA_NUM_PARALLEL` (1), `OLLAMA_MAX_LOADED_MODELS` (3× GPUs), `OLLAMA_MAX_QUEUE` (512), `OLLAMA_NO_CLOUD`, `OLLAMA_ORIGINS` (default permite `127.0.0.1` e `0.0.0.0` — restringir). Endurecimento comum destilado do cofre de coding (notas 261, 293, 294, 297, 299, citadas por nome por estarem fora deste vault): default deny, revalidação no servidor, mTLS/SPIFFE, modelos fixados por digest/assinatura, identidade delegada para RAG/tools, gate formal de produção. Origem: parecer de auditor externo verificado claim a claim nas fontes primárias antes da destilação; a inferência "API keys do Ollama Cloud não protegem a API local" e o escopo exato dos tokens do LM Studio estão marcados como não testados. Ligada em MAPA, [[04-Software/Runtimes]] e [[08-Implementacao-Empresa/Seguranca-empresarial-e-LGPD]]. Impacto: nenhuma recomendação anterior mudou; vLLM segue o serving preferido multiusuário. Nenhum teste próprio executado.
- **2026-09-01 (rev. 5, comparador de workstations):** nova nota [[03-Hardware/Comparativo-workstations-vs-GPU]] (cinco eixos, tabela de seis máquinas, matriz modelo × máquina com modo 1/2/3/4, teto de decode por banda e regras de decisão "compensa 4090/5090?"), primeira ficha de máquina [[03-Hardware/Workstations/Dell-Pro-Max-GB10]] e template [[99-Templates/Modelo-de-ficha-de-workstation]]. Fatos capturados em 2026-09-01: GB10 = 128 GB LPDDR5x a 273 GB/s, 240–280 W, CUDA aarch64, Dell sem preço on-line, DGX Spark 4 TB a R$ 54.552 à vista em revendedor; RTX 5090 = 32 GB GDDR7 a 28 Gbps/512 bits (~1.792 GB/s), TGP 575 W; RTX 4090 = 24 GB, ~1.008 GB/s, TGP 450 W. Conclusão editorial: até 30B o PC com 5090 vence em tokens/s e preço; 70B denso e MoE de 80–125B em modo 1 numa caixa só exigem 128 GB unificados (GB10/Mac) ou 2× 5090; Kimi K2.7 Code não roda em nenhuma. Tabela de classes do Sizing ganhou a linha GB10. Impacto: nenhuma recomendação anterior mudou; tokens/s são tetos de triagem, não medições. Fonte: páginas Dell, NVIDIA, Apple e um revendedor brasileiro; nenhum benchmark próprio executado.
