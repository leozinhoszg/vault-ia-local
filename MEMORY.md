# MEMORY — estado do vault

- **Última atualização:** 1º de setembro de 2026.
- **Escopo:** IA local para casa e empresa; hardware, software, modelos, RAG, treinamento, operação e governança.
- **Dono editorial:** Luiz Guimarães; substituto e dono técnico ainda precisam ser designados.
- **Fonte de verdade de hardware:** `03-Hardware/` e documentação oficial do fabricante.
- **Fonte de verdade de modelos:** model card/repositório oficial; blogs editoriais são contexto, não autoridade única.
- **Fonte de verdade de governança:** NIST AI 600-1 e políticas internas aprovadas.
- **Estado de revisão:** builds e preços são planejamento e precisam de cotação brasileira na data de compra; compatibilidade AMD exige consulta à matriz ROCm vigente.
- **Pendências:** designar substituto, revisor de segurança e operador de benchmarks; preencher região/tarifa de energia, orçamento real, máquina atual e conjunto de benchmarks do usuário.

## Changelog

- **2026-09-01 (rev. 2, re-auditoria P0):** corrigido `Break_even!E2:E4` da planilha TCO (faltava multiplicar o custo blended pelo câmbio; o resultado era ~5,5× maior que o texto); `Local!C2` passou a descontar o valor residual; fator Batch e fatores de contexto longo por categoria agora entram no cálculo; aba `Sensibilidade` ampliada para câmbio, vida útil, utilização e tarifa. Ficha do Qwen3-Coder 30B passou a citar o model card oficial (`Qwen3-Coder-30B-A3B-Instruct`). Script RAG ganhou `--selftest` e `--retrieve-only`; selftest executado em Windows/Python 3.11 com chromadb 1.0.20. Lockfile com hashes adicionado. Cabeçalhos `## Referências` e datas normalizados em 21 notas; validador passou a distinguir "Markdown no pacote" de "analisados" e a aceitar justificativas. Impacto: [[00-Inicio/Auditoria-P0]] reescrita; nenhuma recomendação de hardware ou modelo mudou. Fonte: re-auditoria externa + verificação com `openpyxl` e recomputação em Python.
- **2026-09-01 (rev. 3, fechamento do gate):** RAG reproduzido em venv limpo a partir do lockfile com hashes (selftest + embedding real; Ollama não executado) — evidência em [[07-Implementacao-Casa/Evidencias/RAG-reproducao-2026-09-01]]; planilha TCO ganhou aba `Checks`, anuidade opcional, tabelas estruturadas e valores recalculados pelo Excel (`99-Templates/recalcular_tco.ps1`, conferido por `99-Templates/check_tco.py`); CI em `.github/workflows/validate.yml` (validador `--strict`, índice `--check`, sha256 do lockfile, `check_tco.py`, selftest, release por tag). Itens que exigem hardware (benchmarks, fichas medidas, BOM real, aceitação empresarial) continuam não iniciados e estão listados em [[00-Inicio/Auditoria-P0]].
- **2026-09-01 (rev. 4, enriquecimento de modelos):** novas fichas [[02-Modelos/Fichas/Laguna-XS-2.1]] (33B/3B, 262.144, OpenMDW-1.1, GGUF Q4_K_M de 20,3 GB, Ollama local, llama.cpp PR #25165 mesclado em 2026-07-22) e [[02-Modelos/Fichas/Kimi-K2.7-Code]] (1T/32B, 256K, INT4 nativo de 595 GB, Modified MIT, Ollama apenas `:cloud`; o card não publica SWE-bench). Nova nota [[02-Modelos/Local-real-vs-cloud]] com os quatro modos de execução (local real, offload CPU/RAM, serviço remoto via ferramenta local, Ollama Cloud) e `OLLAMA_NO_CLOUD=1`. Nova [[02-Modelos/Tabela-normalizada-de-benchmarks]] com variante, harness, métrica, tentativas, ferramentas, contexto e data de captura para Laguna XS/S 2.1, Kimi K2.6/K2.7 Code, Qwen3.6-27B e Qwen3-Coder-Next. Ficha padronizada e catálogo ganharam os campos "Modo de execução" e "Arquivo quantizado real". [[02-Modelos/Verificacao-PromptQuorum]] passou a rejeitar `ollama run kimi-k2.6` como prova de inferência local (única tag oficial é `:cloud`), registrou as três correções (1T ≠ 22 GB; 58,6 Pro ≠ 77,2 Verified) e a regra de data de captura por causa das datas internas conflitantes da página. Impacto: nenhuma recomendação de hardware mudou; Laguna XS 2.1 entra como candidato de GPU de 24 GB, Kimi K2.7 Code como infraestrutura/remoto. Fonte: model cards e páginas oficiais capturados em 2026-09-01; nenhum benchmark próprio executado.
- **2026-09-01 (rev. 3b, RAG ponta a ponta):** Ollama 0.33.2 instalado (winget) com `qwen3.5:4b`; pipeline completo reproduzido no venv limpo com 3/3 respostas corretas e citadas, incluindo recusa sem evidência. Defeito corrigido no script: thinking + `num_ctx` 4096 zeravam a resposta (agora `think=false`, `--num-ctx 8192`, exit 3 em resposta vazia). Evidência em [[07-Implementacao-Casa/Evidencias/RAG-reproducao-2026-09-01]].
- **2026-09-01 (rev. 5, hardening do RAG):** a reprodução rev. 3b foi marcada como histórica após identificar advisories nos pins ChromaDB 1.0.20 e pypdf 6.0.0. O exemplo atual (`ffec088e2c2af03ff85d318673b6bcc7ab555539`) usa retrieval cosine efêmero em NumPy, limites fail-closed, fontes relativas, PDF opt-in em subprocesso, Ollama loopback sem proxy/redirect e embedding default fixado por revisão com safetensors. Lock Windows regenerado (43 pins, sha256 `f5256db8…`) e auditado sem vulnerabilidades conhecidas; requirements↔lock agora é gate. Selftest, embedding real e PDF opt-in passaram em container; Windows/Ollama atual permanece pendente. Evidência em [[07-Implementacao-Casa/Evidencias/RAG-hardening-2026-09-01]].
